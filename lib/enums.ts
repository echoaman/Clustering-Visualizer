export enum Algos {
    Kmeans = "kmeans"
};

export enum SecondaryControlButtons {
    AddData = "add-data-btn",
    AddCenter = "add-center-btn",
    RemovePoint = "remove-point-btn",
    None = ""
};